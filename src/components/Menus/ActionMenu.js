import React from 'react'
import { Text, View, StyleSheet } from 'react-native'

const ActionMenu = () => {
    return <View style={styles.container}>
        <Text>This is ActionMenu</Text>
    </View>
}

const styles = StyleSheet.create({
    container: {
    
    }
})

export default ActionMenu ;